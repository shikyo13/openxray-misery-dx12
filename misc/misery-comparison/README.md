# Verified MISERY comparison workflow

The user's current optimization priority and the reviewed Microsoft/NVIDIA/AMD references are recorded in [DX12 engine performance priorities](PERFORMANCE_PRIORITIES.md). Architectural bottlenecks take priority over further graphics additions. DX12 async compute is currently disabled in the renderer; the presence of backend queue support must not be reported as active overlap.

These are versioned copies of the workspace helpers. Run copies from the workspace `work` directory, beside `engine`, `runtime`, `tools`, and `logs`. Game data and save files stay outside this source repository. The reference path in the PowerShell helper is local to the current Windows workspace; adjust it when restoring on another PC.

The DX9 reference is an independent copy of the installed game. It uses an absolute `comparison.ltx`, the existing Steam AppId 41700, and a copied original save. Its `axr_main.script` adds `callback_register("actor_on_update", graphics_comparison_baseline.update)`. DX12 uses the same controller through its native `-run_script` argument and its own compatible save. The staging/copy helpers are one-shot preparation tools and deliberately refuse existing evidence.

Confirmed retail requirements: `keypress_on_start 0`, a nonempty screenshot argument (`screenshot comparison`), and logging markers shorter than the retail console's command buffer. The verified path runs visibly at 3440x1440 with fullscreen enabled. Prior empty screenshot calls merely reported command status. Preserve these constraints when simplifying the controller.

Run `run-graphics-comparison.ps1 -Name NEW_EVIDENCE_NAME -Renderer dx9 -MaxSeconds 260 -Visible -Lossless`, then the equivalent `-Renderer dx12`. Never overlap game instances or a benchmark and compilation. Use distinct evidence names. The report script takes the two evidence-folder names. It rejects failed or incomplete runs and unmatched camera/lens data, trims capture boundaries, and deduplicates log rewrites. The different save formats prevent identical simulation states; up to one minute of game-clock offset is recorded rather than treated as exact synchronization. Screenshots precede driver RTX HDR conversion.

Verified baseline evidence in the workspace: `DX9_MATCHED_041_B` and `DX12_MATCHED_040_A`. Both completed four scenes and exited normally. The original DX9 executable, its settings and tested save pair were preserved. An early Steam-redirected failed attempt did change the original log/cloud metadata; subsequent launches used the isolated context.

The user requires a report refresh after major visual or performance milestones. Keep the DX9 reference fixed, update the main report with the latest tested DX12 build, and retain named snapshots of previous comparisons. The delivered 0.41 build still records sequentially. The experimental `-fg_parallel_record` path now records two certified shadow callbacks on independently owned command lists/contexts and submits them in dependency order. Real CPU overlap was observed, but total recording time did not improve in the first pair and presentation modes differed. It remains disabled by default; see the measured results and priority-1 follow-up in `PERFORMANCE_PRIORITIES.md`. The published DX9/0.41 comparison remains the acceptance baseline.

Reports now include both captured profiles and observed PresentMon presentation modes. Use `--settings-audit MISERY_GRAPHICS_SETTINGS_041.json` for the reviewed 0.41 comparison; the JSON belongs in workspace `outputs` and is bound to the exact run names and executable hashes. Review/update the settings audit for each later build instead of carrying old implementation claims forward. A saved legacy control is not proof that the native renderer uses it. The 0.41 comparison has 16x versus 8x filtering, enabled native grass shadows versus the reference's disabled detail-shadow setting, different AO implementations and different observed presentation paths. Preserve the current DX12 quality preset and label any separate common-settings comparison explicitly; the current FPS gap does not isolate API overhead or the cost of additional effects.

The optional native `graphics_feature_cost.script` measures five settings at each fixed camera: 16x filtering with grass shadows and high AO, 8x with grass shadows, 8x without grass shadows, AO additionally disabled, then the first setting repeated. Keep the normal profile backed up before staging; use 3440x1440, fullscreen, AA off and frame generation off for this comparison. The controller checks the actual console values during every sample. The camera demos must last at least 120 seconds (120 identical matrices at one second per frame). Preserve normal gameplay callbacks and restore the saved profile after the run.

Copy the controller into the isolated native runtime's `gamedata/scripts`, then run the workspace helper with `-Name NEW_FEATURE_EVIDENCE -Renderer dx12 -ScriptNamespace graphics_feature_cost -MaxSeconds 420 -Visible -ExtraArguments '-graphics_trace -shadow_trace'`. It archives fresh GPU profiling data automatically. Add `--feature-probe NEW_FEATURE_EVIDENCE` when generating the comparison report. The report requires a matching executable, all 20 captures, matching camera/lens values and the requested live settings; it flags presentation changes and quality-repeat variation above 5%. The five eight-second samples per scene are trimmed by half a second at each boundary. GPU regions may nest and must not be added together as total GPU time.

Verified evidence `DX12_FEATURE_COST_041_A` exited normally. Interior/outdoor repeats varied by +1.2%/-0.6%; disabling grass shadows at fixed 8x filtering reduced local-shadow GPU time by 1.12/1.50 ms. Lowering filtering alone gave no clear benefit in those samples. Rain changed presentation mode and its repeat varied by +9.7%; night varied by -10.5%. These two sequences cannot isolate small feature costs. The normal profile was restored byte-for-byte; no renderer or shipped quality setting changed. The intermediate settings are diagnostics, not complete DX9 equivalence.


For CPU recording diagnosis, add `-cpu_trace` to the existing helper's `-ExtraArguments`. It enables the same `dx12_graphics.csv` and adds `cpu_stage` and `cpu_pass` rows; GPU and render-interval rows keep their existing schema. CPU stage totals include their child passes and must not be added together. Use the same trace flags on both sides, then verify final performance without diagnostic overhead. The tested early-shadow submission experiment was rejected and removed; its measurements and restoration are recorded in `PERFORMANCE_PRIORITIES.md`. The normal 0.41 package is unchanged.
