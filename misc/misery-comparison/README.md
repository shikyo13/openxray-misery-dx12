# Verified MISERY comparison workflow

Current action: finish authorized storage cleanup and prepare the user's long playtest on delivered 0.46. See [STATUS.md](STATUS.md). The older optimization instructions below are historical and must not restart after compaction.

## Storage and version control

Deploy `storage-policy.json`, `archive-retired-package.py`, `retire-development-packages.ps1`, `package-development.py` and `run-graphics-comparison.ps1` together into the existing workspace `work` directory. Their maintained copies live here in Git. `WORKSPACE_AGENTS.md` is the versioned copy of the workspace-root instructions; `STATUS.md` is copied to `work/progress.md`.

- Keep at most two full packages: current and rollback. Build and test routine changes in the existing isolated runtime; package only a user playtest or release milestone.
- Packaging requires committed source/overlay and an archived build identity. It checks both C: and D: package roots and reserves 30 GiB after copying, with at least 20 GiB free on the system drive. Use `python -X utf8 work/package-development.py --version 0.47 --check-storage` for a read-only preflight; it deliberately refuses a third full package.
- Before creating a replacement package, explicitly retire the older rollback using `work/retire-development-packages.ps1 -Versions @('VERSION') -Apply`. The current version is protected. The helper checks all paths, preserves and verifies an archive, then removes only that package's `game` payload. Original metadata stays beside a retirement receipt. No automatic save deletion.
- Preserve source revisions, patches, manifests and the private compatibility overlay. Do not put game assets, user saves, binary archives or logs into public Git. Keep build artifacts outside source control.
- Short comparisons have duration, disk and evidence limits. Heavy per-frame graphics/AI traces are not suitable defaults for a long playtest.

The 0.1-0.44 package paths below are historical. Their metadata remains available after retirement; preserved saves, settings, binaries and sources are in the archive linked by each `retirement-receipt.json`. Only 0.45 and 0.46 remain runnable full packages.

The user's current optimization priority and the reviewed Microsoft/NVIDIA/AMD references are recorded in [DX12 engine performance priorities](PERFORMANCE_PRIORITIES.md). Architectural bottlenecks take priority over further graphics additions. DX12 async compute is currently disabled in the renderer; the presence of backend queue support must not be reported as active overlap.

These are versioned copies of the workspace helpers. Run copies from the workspace `work` directory, beside `engine`, `runtime`, `tools`, and `logs`. Game data and save files stay outside this source repository. The reference path in the PowerShell helper is local to the current Windows workspace; adjust it when restoring on another PC.

The DX9 reference is an independent copy of the installed game. It uses an absolute `comparison.ltx`, the existing Steam AppId 41700, and a copied original save. Its `axr_main.script` adds `callback_register("actor_on_update", graphics_comparison_baseline.update)`. DX12 uses the same controller through its native `-run_script` argument and its own compatible save. The staging/copy helpers are one-shot preparation tools and deliberately refuse existing evidence.

Confirmed retail requirements: `keypress_on_start 0`, a nonempty screenshot argument (`screenshot comparison`), and logging markers shorter than the retail console's command buffer. The verified path runs visibly at 3440x1440 with fullscreen enabled. Prior empty screenshot calls merely reported command status. Preserve these constraints when simplifying the controller.

Run `run-graphics-comparison.ps1 -Name NEW_EVIDENCE_NAME -Renderer dx9 -MaxSeconds 260 -Visible -Lossless`, then the equivalent `-Renderer dx12`. Never overlap game instances or a benchmark and compilation. Use distinct evidence names. The report script takes the two evidence-folder names. It rejects failed or incomplete runs and unmatched camera/lens data, trims capture boundaries, and deduplicates log rewrites. The different save formats prevent identical simulation states; up to one minute of game-clock offset is recorded rather than treated as exact synchronization. Screenshots precede driver RTX HDR conversion.

Verified baseline evidence in the workspace: `DX9_MATCHED_041_B` and `DX12_MATCHED_040_A`. Both completed four scenes and exited normally. The original DX9 executable, its settings and tested save pair were preserved. An early Steam-redirected failed attempt did change the original log/cloud metadata; subsequent launches used the isolated context.

The user requires a report refresh after major visual or performance milestones. Keep the DX9 reference fixed, update the main report with the latest tested DX12 build, and retain named snapshots of previous comparisons. The delivered 0.41 build still records sequentially. The experimental `-fg_parallel_record` path now records two certified shadow callbacks on independently owned command lists/contexts and submits them in dependency order. Real CPU overlap was observed, but total recording time did not improve in the first pair and presentation modes differed. It remains disabled by default; see the measured results and priority-1 follow-up in `PERFORMANCE_PRIORITIES.md`. The published DX9/0.41 comparison remains the acceptance baseline.

Reports now include both captured profiles and observed PresentMon presentation modes. Use `--settings-audit MISERY_GRAPHICS_SETTINGS_041.json` for the reviewed 0.41 comparison; the JSON belongs in workspace `outputs` and is bound to the exact run names and executable hashes. Review/update the settings audit for each later build instead of carrying old implementation claims forward. A saved legacy control is not proof that the native renderer uses it. The 0.41 comparison has 16x versus 8x filtering, enabled native grass shadows versus the reference's disabled detail-shadow setting, different AO implementations and different observed presentation paths. Preserve the current DX12 quality preset and label any separate common-settings comparison explicitly; the current FPS gap does not isolate API overhead or the cost of additional effects.

The optional native `graphics_feature_cost.script` measures five settings at each fixed camera: 16x filtering with grass shadows and high AO, 8x with grass shadows, 8x without grass shadows, AO additionally disabled, then the first setting repeated. Keep the normal profile backed up before staging; use 3440x1440, fullscreen, AA off and frame generation off for this comparison. The controller checks the actual console values during every sample. The camera demos must last at least 120 seconds (120 identical matrices at one second per frame). Preserve normal gameplay callbacks and restore the saved profile after the run.

Copy the controller into the isolated native runtime's `gamedata/scripts`, then run the workspace helper with `-Name NEW_FEATURE_EVIDENCE -Renderer dx12 -ScriptNamespace graphics_feature_cost -MaxSeconds 420 -Visible -ExtraArguments '-graphics_trace -shadow_trace'`. It archives fresh GPU profiling data automatically. Add `--feature-probe NEW_FEATURE_EVIDENCE` when generating the comparison report. The report requires a matching executable, all 20 captures, matching camera/lens values and the requested live settings; it flags presentation changes and quality-repeat variation above 5%. The five eight-second samples per scene are trimmed by half a second at each boundary. GPU regions may nest and must not be added together as total GPU time.

Verified evidence `DX12_FEATURE_COST_041_A` exited normally. Interior/outdoor repeats varied by +1.2%/-0.6%; disabling grass shadows at fixed 8x filtering reduced local-shadow GPU time by 1.12/1.50 ms. Lowering filtering alone gave no clear benefit in those samples. Rain changed presentation mode and its repeat varied by +9.7%; night varied by -10.5%. These two sequences cannot isolate small feature costs. The normal profile was restored byte-for-byte; no renderer or shipped quality setting changed. The intermediate settings are diagnostics, not complete DX9 equivalence.


For CPU recording diagnosis, add `-cpu_trace` to the existing helper's `-ExtraArguments`. It enables the same `dx12_graphics.csv` and adds `cpu_stage` and `cpu_pass` rows; GPU and render-interval rows keep their existing schema. CPU stage totals include their child passes and must not be added together. Use the same trace flags on both sides, then verify final performance without diagnostic overhead. The tested early-shadow submission experiment was rejected and removed; its measurements and restoration are recorded in `PERFORMANCE_PRIORITIES.md`. The normal 0.41 package is unchanged.

The first parallel preparation bottleneck is now reduced: the required skeletons are prepared from one scene scan per pass, retaining the original visibility union. The short probe reduced this step from 2.48 to 0.25 ms. The following untraced pair did not establish a net FPS gain, and a post-run check found substantial Edge GPU activity with the game closed. Capture background GPU utilization alongside the next performance run; do not infer a tracing regression or a speedup from those runs. The experimental flag remains off by default and the released 0.41 baseline is unchanged.

The versioned `dx12_fsr_fg_probe.script` and `dx12_fsr_fg_resize_probe.script` use the same isolated comparison runner with `-ScriptNamespace`. Copy the selected script into the runtime's `gamedata/scripts` first. They write `_appdata_/dx12_local_shadow_probe.tsv`; archive that file into the named evidence directory after each run, before another probe overwrites it. The normal four-scene report parser does not interpret these custom events. `-graphics_trace` enables generated-frame readback and additional synchronization, so a capture run cannot replace testing the ordinary non-capture path.

For the existing persistence check, copy `run-cop-check.ps1` to workspace `work` and the controller into the isolated runtime. `-Visible` is explicit; hidden launch remains the helper's default. It stages the current built executable/PDB unless `-ReferenceGame` selects a preserved package. `dx12_parallel_persistence_041.script` creates a named save on its first launch and compares inventory and Grouse data when that save is loaded in a second process. Before repeating, choose a new namespace **and replace the controller's internal output/save prefix**; retain old files and saves. Use `-GameArguments` to select the first compatible save and then the newly created test save. Inspect `comparison.txt` and any `error.txt`; exit 0 alone does not prove equality. Archive the result JSON, engine log, controller, profiles and snapshot files. Restore the backed-up normal binary/PDB/profile when finished.

The latest functional follow-up completed 13 AA/FG cases and fresh-process save/reload, but the resize probe failed with parallel recording both on and off and with the preserved 0.41 executable. The raw inventory comparison differed only in the bolt object ID. See `PERFORMANCE_PRIORITIES.md` for the exact results and limits. Parallel recording remains experimental; these tests do not establish full stability, temporal image quality or a net FPS gain.
## Current delivery: 0.46

The private runnable package is `D:/Codex/MISERY-DX12/outputs/MISERY_DX12_DEV_0.46`. It reduces object-removal dispatch cost while retaining native/Lua notifications and order, the A-life budget correction and normal quality settings. The 52-object batch cost fell from 35.149 to 15.240 ms in the same-binary pair; overall FPS and tails are mixed. Per-object multicore simulation remains unfinished. The default launcher uses serial rendering and grouped copies; the optional launcher retains parallel/partitioned recording.

Code identity: `36213bc8e2861271c84a2a0938ef78fcc4f41e87`, built from parent `cb92d5a3f607f67560893f50e69df7973d7c844a` plus the archived patch. Build identity/PDB, launch/save notes and three verification summaries are included. With the matching archived binary/PDB and normal profile staged, reproduce packaging using `python -X utf8 work/package-development.py --version 0.46 --output-root D:/Codex/MISERY-DX12/outputs --batch-shadow-copies --build-identity work/runtime/relcase-cast-046/source-identity.json` only at an unoccupied destination.

For the existing callback probe, copy `dx12_relcase_probe.script` into the isolated runtime scripts folder, preserve any old `_appdata_/dx12_relcase_probe.tsv`, and use the comparison helper with a unique evidence name, `-ScriptNamespace dx12_relcase_probe -ExtraArguments '-relcase_validate -d3d12_diagnostics -local_shadow_batch_copies'`. Check the newly written TSV for all six passes/completion and compare its modification time with the process start; the optional Lua printf is not guaranteed to appear in the engine log. Archive the TSV, native validation log and process result. That probe produces no screenshots. For same-binary four-scene timing, both modes use `-relcase_trace -cpu_trace -frame_trace -local_shadow_batch_copies`; add `-relcase_legacy_cast` only to the control. Use the existing GPU sampler, restore the normal profile between runs, and wait for both processes to terminate before another launch.

CE passed the final native probe; CF/CG completed the measured pair; CH completed the actual normal package run. Twelve final-build stills were opened. The wrong-logger CD attempt was stopped and excluded, then corrected before the reported tests. No new in-process reload or complete-campaign claim is made. See PERFORMANCE_PRIORITIES.md and workspace outputs/MISERY_DX9_DX12_COMPARISON_046.html. Package play settings and protected 0.41 workspace staging are restored; older packages and fixed DX9 remain available.

## Earlier delivery: 0.45

The private runnable package is `D:/Codex/MISERY-DX12/outputs/MISERY_DX12_DEV_0.45`. It restores the authored A-life switching time budget while retaining registry continuation, all objects/callbacks, first-load/precache behavior and the normal scheduled-object count. Per-object multicore simulation remains unfinished. The default launcher retains serial rendering and grouped shadow copies; diagnostics and argument batching are off. A separate launcher keeps the optional parallel/partitioned recorder available.

Source/code identity is `f4a98b7d02dcc830bac84637f316610f2e76c0a0`, compiled from parent `8f0ac7e1b9a3eac395c0f03dc3c4359a3615d893` plus the packaged patch. Build identity, PDB, launch/save instructions and verification summaries are included. After staging the matching archived binary/PDB and protected normal profile, reproduce packaging with `python -X utf8 work/package-development.py --version 0.45 --output-root D:/Codex/MISERY-DX12/outputs --batch-shadow-copies --build-identity work/runtime/alife-budget-045/source-identity.json` only where that version does not already exist.

BY passed two native-validated reloads; BZ/CA compared the same executable's old/fixed budget with longer normal-registry coverage; CB completed four scenes from the actual package without internal diagnostic traces. All 15 screenshots were opened. Total A-life time per invocation fell by about 0.37–0.68 ms through budgeted continuation; overall FPS and tails were mixed. See PERFORMANCE_PRIORITIES.md and workspace outputs/MISERY_DX9_DX12_COMPARISON_045.html. Both the package play profile and protected 0.41 workspace staging were restored and verified. Prior packages and the fixed DX9 reference remain available.

## Earlier delivery: 0.44

The earlier private runnable package is `D:\Codex\MISERY-DX12\outputs\MISERY_DX12_DEV_0.44`. Its normal launcher enables grouped shadow copies and retains serial recording. The optional experimental launcher also enables parallel and partitioned recording. Source/rendering identity is `4bf4438e395e0322d3af3ceab68cb17ba8d8e2f2`; the package includes the exact tested executable/PDB, compiled parent/patch and `build-identity.json`.

Use `python -X utf8 work/package-development.py --version 0.44 --output-root D:/Codex/MISERY-DX12/outputs --batch-shadow-copies --build-identity work/runtime/local-shadow-copy-043/source-identity.json` to reproduce this packaging step only in a destination where that version does not already exist, after staging the matching archived binaries and normal profile. The helper refuses an existing package and verifies the requested binary and patch identity before copying. Existing packages and original saves must remain preserved.

BL tests the actual normal 0.44 package; BM uses the copied 0.43 executable/PDB as a current-condition control in the new runtime directory. Both complete four scenes; all eight stills were inspected. Current 0.44 binaries/profile and protected 0.41 staging were restored and rehashed afterward. See `PERFORMANCE_PRIORITIES.md` and workspace `outputs/MISERY_DX9_DX12_COMPARISON_044.html` for measured gains, settings, runtime evidence and limits. Older entries below are historical; current work follows the largest measured frame-time opportunity.
